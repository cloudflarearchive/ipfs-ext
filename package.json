{
  "private": true,
  "scripts": {
    "build": "browserify src/background.js -t strictify -o build/background.bundle.js",
    "dev": "watchify src/background.js -d -t strictify -o build/background.bundle.js"
  },
  "dependencies": {
    "cids": "^0.7.1",
    "ipfs-unixfs": "^0.1.16",
    "ipld-dag-pb": "^0.17.4",
    "ipns": "^0.5.2",
    "peer-id": "^0.12.2",
    "protons": "^1.0.1"
  },
  "devDependencies": {
    "browserify": "^16.2.3",
    "strictify": "^0.2.0"
  }
}
